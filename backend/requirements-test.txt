# Test dependencies — install in addition to requirements.txt
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0          # already in requirements.txt; listed explicitly for clarity
aiosqlite>=0.20.0      # already in requirements.txt; in-memory SQLite async driver
